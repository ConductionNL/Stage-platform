{% extends "dashboard/base.html.twig" %}

{% block content %}
    <div class="d-lg-flex justify-content-between flex-wrap flex-md-nowrap align-items-center py-4">
        <div class="col-auto d-flex justify-content-between pl-0 mb-4 mb-lg-0">
            <div class="mr-lg-3"><a href="#" class="btn btn-secondary"><span class="fas fa-plus mr-2"></span><span>New Tasks</span></a></div>
            <div class="btn-group"><a class="btn btn-primary text-white" data-toggle="tooltip" data-placement="bottom" title="" data-original-title="Archive"><span class="fas fa-archive"></span></a> <a class="btn btn-primary text-white" data-toggle="tooltip" data-placement="bottom" title="" data-original-title="Report Spam"><span class="fas fa-exclamation-circle"></span></a> <a class="btn btn-primary text-white" data-toggle="tooltip" data-placement="bottom" title="" data-original-title="Delete"><span class="fas fa-trash-alt"></span></a></div>
        </div>
        <div class="col-12 col-lg-6 px-0">
            <div class="form-group mb-0">
                <div class="input-group">
                    <div class="input-group-prepend"><span class="input-group-text"><span class="fas fa-search"></span></span>
                    </div>
                    <input class="form-control" placeholder="Search Tasks Here" type="text">
                </div>
            </div>
        </div>
    </div>

    <div class="border bg-white border-light shadow-sm py-1 rounded">
{% if resources is not empty %}
    {% set counter = 0 %}
    {% for programs in resources %}

        <div class="card hover-state border-bottom rounded-0 py-3">
            <div class="card-body d-sm-flex align-items-center flex-wrap flex-lg-nowrap py-0 pl-sm-0">
                <div class="col-auto col-lg-1 text-left text-sm-center pl-0 pl-sm-4 mb-2 mb-sm-0">
                    <span class="fas fa-books mr-2"></span>
                </div>
                <div class="col-10 col-lg-8 d-block px-0 mb-4 mb-md-0">
                    <div class="mb-2">
                        {% if programs.name is defined and programs.name is not empty %}
                        <h3 class="h5">{{ programs.name }}</h3>
                        {% endif %}
                        <div class="d-block d-sm-flex">
                            <div>{% if programs.startDate is defined and programs.startDate is not empty %}
                                <h4 class="h6 font-weight-normal text-gray mb-3 mb-sm-0"><span class="fas fa-clock mr-2"></span>
                                        startdatum: {{ programs.startDate|date("d-m-Y", "Europe/Paris") }}</h4></div>
                            {% else %}
                            <h4 class="h6 font-weight-normal text-gray mb-3 mb-sm-0"><span class="fas fa-clock mr-2"></span>
                                geen startdatum</h4></div>
                                {% endif %}
                        </div>
                    </div>
                    {% if programs.description is defined and programs.description is not empty %}
                    <div><a href="./single-message.html" class="font-weight-bold text-dark"><span class="font-weight-normal text-gray">{{ programs.description }}</span></a></div>
                    {% endif %}
                </div>
                <div class="col-10 col-sm-2 col-lg-2 col-xl-2 d-none d-lg-block d-xl-inline-flex align-items-center ml-lg-auto text-right justify-content-end px-md-0">
                    <div class="rating-star star-lg d-none d-lg-inline-block">
                        <input type="checkbox" id="InboxStar1" name="star">
                        <label class="rating-star-label" for="InboxStar1"><span class="sr-only">Star</span></label>
                    </div>
                    <div class="btn-group ml-md-3">
                        <button class="btn btn-link text-dark dropdown-toggle dropdown-toggle-split m-0 p-0" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><span class="icon icon-sm"><span class="fas fa-ellipsis-h icon-dark"></span> </span><span class="sr-only">Toggle Dropdown</span></button>
                        <div class="dropdown-menu dropdown-menu-right"><a class="dropdown-item" href="#"><span class="fas fa-edit mr-2"></span> Edit</a> <a class="dropdown-item text-warning" href="#"><span class="fas fa-star mr-2"></span>Important</a> <a class="dropdown-item text-danger" href="#"><span class="fas fa-trash-alt mr-2"></span>Delete</a></div>
                    </div>
                </div>
            </div>
        </div>

    {% endfor %}
{% endif %}
    </div>
{% endblock %}
